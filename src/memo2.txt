フローチャート
・フォームから我慢する事柄と期間を入力して登録ボタンを押す
  (期間は日と時間だけで分以下はいらない。日の上限は無いけど、時の上限は23)
・カウントが表示されるとともにスタートする。
  (○○日○○分○○秒のような感じで)
・期限が過ぎていない時はリセットボタンを表示して、
  過ぎればクリアボタン(達成でも可)を表示する
・それぞれのボタンを押したらメッセージが表示される
  (クリアボタンは祝う感じでリセットボタンは慰めるような感じ)
  リセットボタンのみ本当にリセットして良いのかの確認を取る
・カウントの履歴を残せたら残す